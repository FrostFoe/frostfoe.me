// This file is no longer needed for pagination on the home page.
// The home page now handles its own pagination with a "Load More" button.
// You can safely delete this file.
// If you want to keep it for other paginated sections, you'll need to adapt it.

const BlogPagination = () => {
  return null;
};

export default BlogPagination;

export async function generateStaticParams() {
  return [];
}
